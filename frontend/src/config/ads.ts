/**
 * Настройки рекламных блоков Яндекс.РСЯ.
 *
 * Идентификаторы блоков создаются в кабинете partner.yandex.ru и выглядят
 * как R-A-XXXXXXXX-N. Здесь они собраны в одном месте, чтобы не искать их
 * по коду: поменяли строку — поменялся блок на странице.
 *
 * Про отдельные блоки на каждое место: один и тот же идентификатор можно
 * без проблем использовать на разных страницах, потому что одновременно
 * на экране он всё равно один. Но статистику по местам вы тогда не разделите,
 * поэтому под ленту задач лучше завести в кабинете свой блок и вписать его
 * в TASKS_FEED — формат «Лента» или «Адаптивный» подойдёт лучше всего.
 */

/** Баннер под формой входа. */
export const AD_BLOCK_LOGIN = 'R-A-20075461-1';

/** Блок внутри списка задач (нативная карточка в общей сетке). */
export const AD_BLOCK_TASKS_FEED = 'R-A-20075461-1';

/**
 * Общий выключатель. Пригодится, если рекламу нужно быстро убрать
 * со всего приложения, не выкорчёвывая компоненты.
 */
export const ADS_ENABLED: boolean = true;
